{
  "name": "mel",
  "version": "0.1.0",
  "description": "Package for building and running MEL website.",
  "author": "NL",
  "license": "MIT",
  "devDependencies": {},
  "scripts": {
    "start": "jekyll serve -P 8888",
    "build": "node scripts/whale.js && bash scripts/ec-process.sh && jekyll build"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "jsdom": "^16.2.2"
  }
}
